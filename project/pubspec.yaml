name: project
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # Core Firebase packages
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.1.0
  firebase_messaging: ^15.1.0
  animate_do: ^3.3.4

  # State management
  provider: ^6.1.2

  # UI & UX Enhancements
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.0
  fluttertoast: ^8.2.4
  lottie: ^3.1.0

  # Optional (for future extensions)
  # google_maps_flutter: ^2.7.0   # Only if you later add event location maps

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Uncomment and use these later if you add images or fonts
  # assets:
  #   - assets/images/
  #
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
