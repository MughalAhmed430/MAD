## Generated by Flutter
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1536M

# AndroidX packaging and Jetifier settings
android.enableR8=false
android.useAndroidX=true
android.enableJetifier=true

# Flutter multidex
android.multidex.enabled=true

# Kotlin code style for this project: Kotlin coding conventions
kotlin.code.style=official

# Disable build features to avoid issues
android.defaults.buildfeatures.buildconfig=false
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.resvalues=false
android.defaults.buildfeatures.shaders=false

# Build cache settings
org.gradle.caching=true

# Parallel execution for faster builds
org.gradle.parallel=true
org.gradle.daemon=true

# Disable pre-dexing for multidex
android.disableDexVerifyArtProfile=true

# Flutter-specific
flutter.enableR8=false
flutter.minSdkVersion=21
flutter.targetSdkVersion=34
flutter.compileSdkVersion=34