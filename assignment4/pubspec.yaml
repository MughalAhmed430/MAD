name: assignment4
description: "SmartTracker - Activity Tracking App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.0
  firebase_auth: ^4.14.0
  cloud_firestore: ^4.9.0
  provider: ^6.1.1
  http: ^1.1.0
  geolocator: ^11.0.0
  camera: ^0.10.5
  google_maps_flutter: ^2.5.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4
  permission_handler: ^11.0.1
  connectivity_plus: ^5.0.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Google Maps API key configuration
